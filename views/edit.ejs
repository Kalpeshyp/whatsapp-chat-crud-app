<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>edit chat</title>
  </head>
  <body>
    <h3>edit you message :<%= chat.id %></h3>
    <p>this msg share from <b><%= chat.from %></b> to <b><%= chat.to %> </b></p>
    <form method="post" action="/chats/<%= chat.id %>?_method=PUT">
      <textarea name="msg" cols="15" rows="14"><%= chat.msg %></textarea><br />
      <button>edit</button>
    </form>
    <br />
  </body>
</html>
